#include <stdio.h>

int main(void) {

    float nota1 = 10;
    float nota2 = 5;
    float media = (nota1 + nota2) / 2;

    //desvio simples, por UMA única condição
    if(media >= 6) {
        printf("Aprovado miseravi!");
    } else {
        
    }

    return 0;
}

